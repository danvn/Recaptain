var Rebound = require('reboundodm');

//Rebound.connect('localhost:9200');


/*var messageSchema = Reboud.Schema({
  _id: { type: Number, default: 0 },
  message: {
    type: Object,
    properties: {
      text: {
        type: String,
        analyzer: 'not_analyzed'
      }
    }
  }
});*/

//var message = Reboud.modelIndex()
